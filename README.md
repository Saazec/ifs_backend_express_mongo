# ifs_backend_express_mongo
created using Node Express and MongoDB
> Instructions
- clone the project
- from the terminal, navigate to ifs_backend_express_mongo
- type `npm install`
- `npm run dev` for development server and, `npm run debug` for debugging
